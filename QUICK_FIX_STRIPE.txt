╔══════════════════════════════════════════════════════════════════════╗
║  STRIPE SUBSCRIPTION UPGRADE - QUICK FIX GUIDE                       ║
╚══════════════════════════════════════════════════════════════════════╝

🔴 ERROR: 406 (Not Acceptable) when upgrading subscription
📍 Location: system_config table RLS policies blocking access

┌──────────────────────────────────────────────────────────────────────┐
│ ⚡ QUICK FIX (2 minutes)                                              │
└──────────────────────────────────────────────────────────────────────┘

1️⃣  Open Supabase SQL Editor:
   → https://supabase.com/dashboard/project/anlivujgkjmajkcgbaxw/sql/new

2️⃣  Copy/paste this file:
   → FIX_STRIPE_PUBLIC_KEY_RLS.sql
   
3️⃣  Click "Run" ▶️

4️⃣  Update Stripe public key:
   ```sql
   UPDATE public.system_config 
   SET value = '"pk_test_YOUR_KEY_HERE"'::jsonb
   WHERE key = 'stripe_public_key';
   ```
   
   Get your key from: https://dashboard.stripe.com/test/apikeys

5️⃣  Test at: https://www.eventnexus.eu/#/pricing

┌──────────────────────────────────────────────────────────────────────┐
│ 📋 CHECKLIST                                                          │
└──────────────────────────────────────────────────────────────────────┘

Database (system_config table):
  □ stripe_public_key = "pk_test_..." or "pk_live_..."
  
Edge Function Secrets (Settings → Functions):
  □ STRIPE_SECRET_KEY
  □ STRIPE_WEBHOOK_SECRET
  □ STRIPE_PRICE_PRO
  □ STRIPE_PRICE_PREMIUM
  □ STRIPE_PRICE_ENTERPRISE

┌──────────────────────────────────────────────────────────────────────┐
│ 🧪 VERIFICATION                                                       │
└──────────────────────────────────────────────────────────────────────┘

Run in SQL Editor:
```sql
-- Check if key is accessible
SELECT value FROM public.system_config 
WHERE key = 'stripe_public_key';
```

Expected: Returns JSON string like "pk_test_..."
❌ Error: Means RLS policy didn't apply correctly

┌──────────────────────────────────────────────────────────────────────┐
│ 🔍 TROUBLESHOOTING                                                    │
└──────────────────────────────────────────────────────────────────────┘

Still 406 error?
  → Re-run FIX_STRIPE_PUBLIC_KEY_RLS.sql
  → Check policies: SELECT * FROM pg_policies WHERE tablename='system_config'

"Stripe public key not configured"?
  → Key is placeholder or missing
  → Update with real key from Stripe Dashboard

Checkout page fails?
  → Edge Function secrets missing
  → Set using: npx supabase secrets set ...

"Subscription price not configured"?
  → Price IDs not in Edge Function secrets
  → Run: bash fix-subscription-checkout.sh

┌──────────────────────────────────────────────────────────────────────┐
│ 📚 FULL DOCUMENTATION                                                 │
└──────────────────────────────────────────────────────────────────────┘

→ STRIPE_SUBSCRIPTION_ERROR_FIX.md (detailed guide)
→ docs/STRIPE_SETUP.md (complete Stripe integration)
→ SUBSCRIPTION_CHECKOUT_FIX.md (previous fixes)

┌──────────────────────────────────────────────────────────────────────┐
│ 💡 WHY THIS HAPPENED                                                  │
└──────────────────────────────────────────────────────────────────────┘

Two migrations created conflicting RLS policies:
1. 20250119000003 → Allowed authenticated read ✅
2. 20241221000004 → Restricted to admin only ❌ (applied later)

Result: Regular users couldn't read stripe_public_key → 406 error

Fix: New policy allows ANYONE to read stripe_public_key specifically

╔══════════════════════════════════════════════════════════════════════╗
║  SUPPORT: huntersest@gmail.com                                       ║
╚══════════════════════════════════════════════════════════════════════╝
